Look at the code in the `webapp` folder. I personally haven't looked at it yet. I recently learned about code potentially being in multiple small silos with numerous interfaces to deal with (BAD) vs being in large modules with simple interfaces (GOOD) - "grey box" kind of thing where the interface is simple, the output is stable and consistent, and we don't need to look into the box. 

Looking at the code, do you think they are in small silos?

Is there room for improvement? ("No" is a valid answer. Don't fix what isn't broken and is already refactored enough.)

Perform an assessment and give me your plan is you think improvements can be made.